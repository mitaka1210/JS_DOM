function solve (x, y, z) {

    let summ = x.length + y.length + z.length;
    let avg = Math.floor(summ / 3 );

    console.log(`${summ}`)
    console.log(`${avg}`)
}

solve('ice crean', 'chocolate', 'cake');
